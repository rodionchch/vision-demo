import styled from 'styled-components/native';
import {List, Text} from 'react-native-paper';

export const Folders = styled(List.Section)``;

export const FoldersHeader = styled(List.Subheader)``;

export const FoldersItem = styled(List.Item)``;

export const FoldersItemIcon = styled(List.Icon)`
  padding-left: 16px;
`;

export const FoldersItemText = styled(Text)``;
