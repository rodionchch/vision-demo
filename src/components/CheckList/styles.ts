import styled from 'styled-components/native';
import {Checkbox, List} from 'react-native-paper';

export const CheckList = styled(List.Section)``;

export const CheckListItem = styled(Checkbox.Item)``;
