import React from 'react';

import * as s from './styles';

const MailActions = () => {
  return (
    <s.MailActions>
      <s.MailAction icon="reply" onPress={() => {}} />
      <s.MailAction icon="reply-all" onPress={() => {}} />
      <s.MailAction icon="share" onPress={() => {}} />
      <s.MailAction icon="delete" onPress={() => {}} />
      <s.MailAction icon="heart" onPress={() => {}} />
    </s.MailActions>
  );
};

export default MailActions;
