import styled from 'styled-components/native';
import {ScrollView} from 'react-native-gesture-handler';
import {View} from 'react-native';

export const SmsList = styled(ScrollView)``;

export const SmsListPlug = styled(View)`
  height: 60px;
`;
