import styled from 'styled-components/native';
import {Text} from 'react-native-paper';

export const NavigationBarSubtitle = styled(Text)`
  position: absolute;
  left: 0;
  right: 0;
  margin: auto;
  text-align: center;
  bottom: -5px;
`;
