import styled from 'styled-components/native';
import {ScrollView} from 'react-native-gesture-handler';
import {View} from 'react-native';

export const MailList = styled(ScrollView)``;

export const MailListPlug = styled(View)`
  height: 60px;
`;
